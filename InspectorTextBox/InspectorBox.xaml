<UserControl x:Class="InspectorTextBox.InspectorBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectorTextBox"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Parent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="{Binding ElementName=NotifyLabel, Path=Height}"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputTextBox"
                 Style="{Binding ElementName=Parent, Path=TextBoxStyle}"
                 Text="{Binding ElementName=Parent, Path=ValueContainer.Value, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock x:Name="NotifyLabel" 
                   Grid.Row="1"
                   Style="{Binding ElementName=Parent, Path=NotificationStyle}"
                   Text="{Binding ElementName=Parent, Path=ValueContainer.Notification}"/>
    </Grid>
</UserControl>
